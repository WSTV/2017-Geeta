<div class="container">
<h1>Listing all videos in the database.</h1>

<p>This page enables you to see each video's details and to delete the video altogether from the system if unwanted.</p>

<table class="table table-hover">
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Video link id</th>
    <th>Details</th>
    <th>Delete</th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.title %></td>
    <td><%= item.description %></td>
    <td><%= item.link %></td>
    <td><%= link_to 'Show', item %></td>
    <td><%= link_to 'Destroy', item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Publish a New Video? >>', new_item_path %>

</div>
